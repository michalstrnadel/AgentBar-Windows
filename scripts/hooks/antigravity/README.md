# Antigravity

Antigravity is an IDE, not a CLI, so there is no hook script and no keystroke path
(`ApproveKeys` is null). It is listed in `AgentCatalog` for the brand mark and for
opening the app; live status would require an editor-side integration.
